# i~ j 까지 숫자에서 K번째 데이터를 찾아라
def solution(array, commands):
    answer = []
    for c in commands: 
        i, j, k = c
        new_array = sorted(array[i-1:j])
        answer.append(new_array[k-1])
    return answer
